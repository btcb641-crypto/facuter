# 📋 دليل رفع التطبيق على GitHub وتشغيله

## 📌 المتطلبات الأساسية

- حساب على [GitHub](https://github.com)
- تثبيت [Git](https://git-scm.com/downloads)
- تثبيت [Node.js](https://nodejs.org) (إصدار 18 أو أعلى)

---

## 🔧 الخطوة 1 — إعداد Git محلياً (مرة واحدة فقط)

```bash
git config --global user.name "اسمك"
git config --global user.email "بريدك@example.com"
```

---

## 📁 الخطوة 2 — إنشاء مستودع على GitHub

1. اذهب إلى [github.com/new](https://github.com/new)
2. اسم المستودع: `invoice-manager` (أو أي اسم تريده)
3. اختر **Public**
4. **لا** تضع علامة على "Add README"
5. اضغط **Create repository**

---

## 📤 الخطوة 3 — رفع الكود

افتح Terminal/CMD في مجلد المشروع ونفّذ:

```bash
# تهيئة Git
git init

# إضافة كل الملفات
git add .

# أول commit
git commit -m "🚀 Initial commit — نظام إدارة الفواتير"

# ربط بـ GitHub (غيّر YOUR_USERNAME و YOUR_REPO)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# رفع الكود
git branch -M main
git push -u origin main
```

---

## 🌐 الخطوة 4 — تفعيل GitHub Pages

### الطريقة التلقائية (GitHub Actions) ✅ موصى به

1. بعد رفع الكود، اذهب إلى مستودعك على GitHub
2. اضغط **Settings** (الإعدادات)
3. من القائمة الجانبية اضغط **Pages**
4. في قسم **Build and deployment**:
   - **Source**: اختر `GitHub Actions`
5. انتظر دقيقة حتى يعمل الـ Action
6. ستجد رابط التطبيق: `https://YOUR_USERNAME.github.io/YOUR_REPO/`

---

## 🔍 الخطوة 5 — مراقبة النشر

1. اذهب إلى تبويب **Actions** في مستودعك
2. ستجد workflow اسمه **Deploy to GitHub Pages**
3. انتظر حتى تصبح الدائرة خضراء ✅
4. افتح رابط التطبيق!

---

## 🔄 تحديث التطبيق لاحقاً

عند عمل أي تعديل:

```bash
git add .
git commit -m "✏️ وصف التعديل"
git push
```

سيتم النشر تلقائياً خلال دقيقة!

---

## ❓ حل المشاكل الشائعة

### الصفحة تظهر 404
- تأكد من تفعيل Pages من Settings
- انتظر 2-3 دقائق بعد النشر
- تأكد أن الـ Action نجح (خضراء ✅)

### خطأ عند git push
```bash
# إذا طلب username وpassword، استخدم Personal Access Token
# اذهب إلى: Settings → Developer settings → Personal access tokens
```

### npm install بطيء
```bash
# استخدم npm بدلاً من yarn
npm install --legacy-peer-deps
```

---

## 💡 نصائح

- احفظ رابط التطبيق في المفضلة
- يمكن مشاركة الرابط مع أي شخص
- البيانات محفوظة في المتصفح (localStorage) — خاصة بكل جهاز
- لنقل البيانات بين أجهزة، يمكن إضافة خاصية تصدير/استيراد JSON مستقبلاً

---

## 📱 فتح على الهاتف

بعد النشر، افتح الرابط على هاتفك:
`https://YOUR_USERNAME.github.io/YOUR_REPO/`

يعمل مباشرة بدون تثبيت أي شيء! ✅
