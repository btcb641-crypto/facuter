# 📤 دليل رفع التطبيق على GitHub — خطوة بخطوة

## ✅ ما الذي تحتاج رفعه؟

### 📁 هيكل المجلدات الكامل الذي يجب رفعه:

```
invoice-manager/           ← المجلد الرئيسي (ارفع كل شيء داخله)
│
├── 📄 public/             ← ⭐ المهم جداً (التطبيق المستقل HTML+CSS+JS)
│   ├── index.html
│   ├── app.js
│   ├── app.css
│   ├── manifest.json
│   ├── sw.js
│   └── icon.svg
│
├── 📁 src/                ← كود React (مطلوب للبناء التلقائي)
│   ├── App.tsx
│   ├── main.tsx
│   ├── index.css
│   ├── store.ts
│   ├── types.ts
│   ├── utils/
│   └── components/
│
├── 📁 android/            ← كود Android (مطلوب لبناء APK)
│   ├── app/
│   ├── gradle/
│   ├── build.gradle
│   ├── settings.gradle
│   ├── gradle.properties
│   └── gradlew
│
├── 📁 .github/
│   └── workflows/
│       ├── deploy.yml       ← نشر GitHub Pages تلقائي
│       └── build-apk.yml    ← بناء APK تلقائي
│
├── package.json
├── vite.config.ts
├── tsconfig.json
├── capacitor.config.ts
├── .gitignore
└── README.md
```

---

## 🚀 الخطوات التفصيلية

### الخطوة 1️⃣ — تثبيت Git (إذا لم يكن مثبتاً)

1. اذهب إلى: **https://git-scm.com/downloads**
2. حمّل النسخة المناسبة لنظامك (Windows / Mac / Linux)
3. ثبّت بالإعدادات الافتراضية
4. تحقق من التثبيت:
```bash
git --version
# يجب أن يظهر: git version 2.x.x
```

---

### الخطوة 2️⃣ — إنشاء حساب GitHub (إذا لم يكن لديك)

1. اذهب إلى: **https://github.com**
2. اضغط **Sign up**
3. أدخل: البريد الإلكتروني، كلمة المرور، اسم المستخدم
4. فعّل الحساب من البريد الإلكتروني

---

### الخطوة 3️⃣ — إنشاء مستودع جديد

1. اذهب إلى: **https://github.com/new**
2. أدخل اسم المستودع: `invoice-manager`
3. اختر: ✅ **Public**
4. **لا تضع علامة** على أي خيار آخر
5. اضغط: **Create repository**
6. **احفظ الرابط الذي يظهر** مثل:
   ```
   https://github.com/YOUR_USERNAME/invoice-manager.git
   ```

---

### الخطوة 4️⃣ — فتح Terminal في مجلد المشروع

**Windows:**
- افتح مجلد المشروع في المستكشف
- اضغط كليك يمين داخل المجلد
- اختر: **"Open in Terminal"** أو **"Git Bash Here"**

**Mac:**
- افتح Terminal
- اكتب: `cd ` ثم اسحب المجلد إلى Terminal واضغط Enter

---

### الخطوة 5️⃣ — إعداد Git (مرة واحدة فقط)

```bash
git config --global user.name "اسمك بالإنجليزي"
git config --global user.email "بريدك@gmail.com"
```

---

### الخطوة 6️⃣ — رفع الكود (نفّذ هذه الأوامر بالترتيب)

```bash
# 1. تهيئة Git في المجلد
git init

# 2. إضافة كل الملفات
git add .

# 3. حفظ أول نسخة
git commit -m "نظام إدارة الفواتير - النسخة الأولى"

# 4. ربط بـ GitHub (غيّر YOUR_USERNAME باسم حسابك)
git remote add origin https://github.com/YOUR_USERNAME/invoice-manager.git

# 5. تحديد الفرع الرئيسي
git branch -M main

# 6. رفع الكود
git push -u origin main
```

> ⚠️ **مهم:** عند طلب username وpassword في Windows:
> - Username: اسم حساب GitHub
> - Password: استخدم **Personal Access Token** (وليس كلمة المرور)
> - طريقة إنشاء Token: GitHub → Settings → Developer settings → Personal access tokens → Generate new token → اختر كل الصلاحيات → Copy

---

### الخطوة 7️⃣ — تفعيل GitHub Pages (موقع ويب)

1. افتح مستودعك على GitHub
2. اضغط تبويب **⚙️ Settings**
3. من القائمة الجانبية اضغط **Pages**
4. في قسم **Build and deployment**:
   - **Source**: اختر `GitHub Actions`
5. انتظر **1-2 دقيقة**
6. رابط موقعك:
   ```
   https://YOUR_USERNAME.github.io/invoice-manager/
   ```

---

### الخطوة 8️⃣ — بناء APK (تطبيق أندرويد)

1. افتح مستودعك على GitHub
2. اضغط تبويب **Actions**
3. من القائمة الجانبية اختر **📱 Build Android APK**
4. اضغط **Run workflow** (الزر الأخضر)
5. اختر نوع البناء: **debug**
6. اضغط **Run workflow**
7. انتظر **15-25 دقيقة**
8. عند الانتهاء، اضغط على البناء ← تبويب **Releases** ← حمّل ملف `.apk`

---

## 🔄 تحديث التطبيق لاحقاً

عندما تعدّل أي ملف، نفّذ فقط:

```bash
git add .
git commit -m "وصف التعديل الذي قمت به"
git push
```

✅ سيتم تحديث الموقع تلقائياً خلال دقيقة!

---

## ❓ حل المشاكل الشائعة

### ❌ "git is not recognized"
- أعد تشغيل Terminal بعد تثبيت Git

### ❌ "Permission denied" عند git push
- استخدم Personal Access Token بدلاً من كلمة المرور

### ❌ الموقع يظهر 404
- تأكد من تفعيل Pages من Settings
- انتظر 3 دقائق بعد النشر

### ❌ APK لا يُثبَّت على الهاتف
- فعّل "مصادر غير معروفة" في إعدادات الأمان
- تأكد من Android 6.0+

### ❌ البناء فشل في Actions
- اضغط على البناء الفاشل
- اقرأ الخطأ في الخطوة الحمراء
- جرّب تشغيل البناء مرة أخرى

---

## 💡 نصائح مهمة

| الموضوع | التفاصيل |
|---------|----------|
| 💾 **البيانات** | محفوظة في ذاكرة المتصفح (localStorage) — خاصة بكل جهاز |
| 📱 **PWA** | افتح الموقع في Chrome → "إضافة للشاشة الرئيسية" |
| 🔒 **الأمان** | الموقع يعمل بـ HTTPS تلقائياً على GitHub Pages |
| 🌐 **المشاركة** | الرابط يعمل على أي جهاز في العالم |
| 📊 **تحديث** | عند أي تعديل → git add . && git commit && git push |
